// Arrow function to get the length of a string
const getStringLength = str => str.length;

// Example usage
const myString = "Hello, World!";
const length = getStringLength(myString);

console.log(`The length of the string "${myString}" is: ${length}`);
